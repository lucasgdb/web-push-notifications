{
  "name": "web-push-notifications",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --ignore-path .prettierignore --write --check ."
  },
  "devDependencies": {
    "prettier": "^2.8.3",
    "typescript": "^4.9.4"
  }
}
